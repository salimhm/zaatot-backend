import { createTool } from '@voltagent/core'

const medicTool = createTool({
  name: 'medic',
  description: 'A tool for medical assistance and information.',
  execution: async () => {
    // Implement the logic for the medic tool here
  },
})
